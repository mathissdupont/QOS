[package]
name = "qos-ui"
version = "0.1.0"
edition = "2021"
description = "Portable modern-UI core for QOS: true-color compositor surface, drawing primitives, dirty-rect tracking, light/dark themes. Host-tested. See ADR-0017 / WP-05."
license = "AGPL-3.0-only"

[lib]
# `no_std` in the kernel, `std` under `cargo test` (via `#![cfg_attr(not(test), no_std)]`).
